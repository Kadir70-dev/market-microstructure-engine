# Runs the cleanup weekly (Sunday 03:00 local), catching up if the box was off.

[Unit]
Description=Weekly mme-engine disk cleanup

[Timer]
OnCalendar=Sun *-*-* 03:00:00
Persistent=true

[Install]
WantedBy=timers.target
